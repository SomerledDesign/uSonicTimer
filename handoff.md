## Handoff

1. Latest request: add handoff.md, drop WiFi/network feature, keep debug env as default.
2. Current status: no code changes; documentation update only.
3. Decisions:
   - Network/WiFi features are not needed for this project.
   - Keep `debug` as the default PlatformIO environment.
4. Open questions: none.
